<!-- index map view for when user is logged in -->
<div class="col-md-8 maps">
  <div id="map-container">
    <div id="map-canvas">
      <%= render 'map' %>
    </div>
  </div>
</div>

<!-- listing info view for logged in users -->
 <div class="col-md-4 listings">
    <div class='ovaflowing'>
      <% @listings.each do |f| %>
        <div class="listing-each">
          <p><%= f.type %> | <%= f.address %>, <%= f.city.capitalize %>, <%= f.state %> <%= f.zip %> | Bedrooms: <%= f.num_bed %> | Bathrooms: <%= f.num_bath %> | $<strong><%= f.price %></strong></p>
          <p><%= link_to 'More', listing_path(f), :class => "btn btn-info btn-sm" %></p> 
          
<!--           <p><%#=  image_tag f.photo.url(:small), height: '90', width: '80' %></p> --> 
        </div>
     <% end %>
    </div>

 </div>


    <!-- listing info view for not logged in users -->
      
       <!--  <table>
          <tbody>
            <table class="table table-hover">
              <%# @listings.each do |f| %>
              <tr>
                  <td><%#= f.type %></td>
                  <td><%#= f.address %></td>
                  <td><%#= f.city.capitalize %> 
                  <td><%#= f.state %></td>
                  <td><%#= f.zip %></td>
                  <td><%#= f.num_bed %></td>
                  <td><%#= f.num_bath %></td>
                  <td>$<#%= f.price %></td>
                  <td><%#= f.description %></td>
                  <td><%#= image_tag f.photo.url(:small), height: '150', width: '130' %></td>
                  <%#if current_user %>
                  <td><%#= link_to 'See More', listing_path(f), :class => "btn btn-info" %></td>
                  <%# else %>
                  <td><%#= link_to "Login to see more", login_path, :class => "btn btn-info" %></td>
                  <%# end %>
                </tr>
                <%# end %>
              </table>
            </tbody>
          </table>
          <%#end%>
  
  </div>
</div>






